name: PowerShell validation

on:
  push:
  pull_request:

jobs:
  validate:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v6
      - name: Parse scripts and run static tests
        shell: powershell
        run: .\tests\Static.Tests.ps1
